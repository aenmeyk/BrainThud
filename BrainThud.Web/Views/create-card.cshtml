@{
    ViewBag.EditorName = "create";
}

<section id="create-card-view" class="view">

    @Html.Partial("_card-editor")

    <div class="row-fluid">
        <div class="span12">
            <button type="button" class="btn right-align" data-bind="click: createCard">Create & New</button>
        </div>
    </div>
</section>
