@{
    ViewBag.Title = "BrainThud";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.HideNavBar == null || !ViewBag.HideNavBar)
{
    <text>
        @section menu {
            <li>@Html.ActionLink("Library", "Index", "Library")</li>
        }
    </text>
}

@RenderBody()

@section Scripts {
    @RenderSection("scripts", required: false)
}
