define('vm.quiz', ['ko'],
    function (ko) {
        var value= '',
            activate = function (){};

        return {
            value: value,
            activate: activate
        };
    }
);
