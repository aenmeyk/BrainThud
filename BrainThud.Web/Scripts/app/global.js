define('global', [],

function () {
    var
        userId = '',
        previousUrl = '',
        currentUrl = '';

    return {
        userId: userId,
        previousUrl: previousUrl,
        currentUrl: currentUrl
    };
});